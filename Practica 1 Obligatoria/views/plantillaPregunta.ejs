<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="img/png" href="/recursos/404icon.png">
    <title>Document</title>
</head>

<body>
    <p class="text">
        <%=pregunta.texto%>
    </p>

    <% if (pregunta.tags !="" ) { %>
        <div class="tags">
            <div>
                <% pregunta.tags.forEach(function(t) { %>
                    <span class="tag">
                        <a href="/preguntas/etiquetados/<%=t%>">
                            <%=t%>
                        </a>
                    </span>
                    <% }); %>
            </div>
        </div>
        <% } %>

            
</body>

</html>